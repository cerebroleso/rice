# Desktop Hardware Configuration (Placeholder)
# Run `sudo nixos-generate-config --show-hardware-config > hosts/desktop/hardware-configuration.nix` on your desktop PC
{ config, lib, pkgs, modulesPath, ... }:

{
  imports = [ ];
}
